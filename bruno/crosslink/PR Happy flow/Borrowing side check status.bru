meta {
  name: Borrowing side check status
  type: http
  seq: 12
}

get {
  url: {{host}}/patron_requests/{{prId}}?side=borrowing
  body: none
  auth: basic
}

params:query {
  side: borrowing
}

headers {
  X-Okapi-Tenant: {{OkapiTenantReq}}
}

auth:basic {
  username: {{userName}}
  password: {{userPassword}}
}

script:post-response {
  test("Check if found lending PR", () => {
      expect(res.getBody().State).to.eql("COMPLETED");
  });
}

settings {
  encodeUrl: true
}
