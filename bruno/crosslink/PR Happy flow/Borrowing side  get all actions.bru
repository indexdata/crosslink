meta {
  name: Borrowing side  get all actions
  type: http
  seq: 39
}

get {
  url: {{host}}/patron_requests/{{prId}}/actions?side=borrowing
  body: none
  auth: basic
}

params:query {
  side: borrowing
}

headers {
  X-Okapi-User-Id: {{userName}}
  X-Okapi-Tenant: {{OkapiTenantReq}}
}

auth:basic {
  username: {{userName}}
  password: {{userPassword}}
}

settings {
  encodeUrl: true
  timeout: 0
}
